import React from 'react'

function ResetPassword() {
  return (
    <div className='fixed inset-0 z-50 flex items-center justify-center p-4'>
      {/* <div className='absolute inset-0 bg-black/60 backdrop-blur-sm'></div>
      <div className='relative w-full max-w-md  bg-slate-800/95 backdrop-blur-xl rounded-lg shadow-2xl border border-slate-600 overflow-hidden animate-in fade-in zoom-in duration-200'> */}
      {/* { isLoading && (
        <div className='absolute top-1'></div>
      )

      } */}
      {/* </div> */}
    </div>
  )
}

export default ResetPassword
